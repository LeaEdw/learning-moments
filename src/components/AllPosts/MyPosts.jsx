import "./AllPosts.css"

export const MyPosts = ({currentUser}) => {

    
    return (
        <div className="myPosts-container">
            <h1>This page will hold the user's posts</h1>
        </div>
    )
}